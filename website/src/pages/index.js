/**
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @format
 */

import React from 'react';
import clsx from 'clsx';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: 'Rapid Experimentation',
    // imageUrl: 'img/undraw_docusaurus_mountain.svg',
    description: (
      <>
        Use out of the box pre-trained models relating to touch sensing directly
        in your experiments. Focus on what matters through re-usable building
        blocks provided by PyTouch.
      </>
    ),
  },
  {
    title: 'Extendable',
    // imageUrl: 'img/undraw_docusaurus_tree.svg',
    description: (
      <>
        Use PyTouch as a base for all future experiments through extending the
        library with additional tasks and models.
      </>
    ),
  },
  {
    title: 'Powered by PyTorch',
    // imageUrl: 'img/undraw_docusaurus_react.svg',
    description: (
      <>
        PyTouch is built upon PyTorch which allows for rapid experimentation
        with projects related to touch sensing and manipulation. PyTouch is
        easily extendable via the PyTorch library.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={clsx('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

export default function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`${siteConfig.title}`}
      description="A Machine Learning Library for Touch Processing">
      <header className={clsx('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={clsx(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/')}>
              Get Started
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length > 0 && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map(({title, imageUrl, description}) => (
                  <Feature
                    key={title}
                    title={title}
                    imageUrl={imageUrl}
                    description={description}
                  />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}
